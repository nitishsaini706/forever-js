<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    
    <h1>Edit a product</h1>

    <form action="/products/<%= product._id %>/edit?_method=PUT" method="POST">

        <label for="name">Name</label>
        <input type="text" id="name" name='name' placeholder="name" value="<%= product.name %> ">
        <label for="price">price</label>
        <input type="number" id="price" name='price' placeholder="price" value="<%= product.price %>">
        <label for="category"></label>
        <select name="category" id="category">
            <option value="<%= product.category === 'fruit' ? 'fruit' : product.category %>" selected>fruit </option>
            <option value="<%= product.category === 'vegetable' ? 'vegetable' : product.category %>" selected >vegetable</option>

            <option value="<%= product.category === 'dairy' ? 'dairy' : product.category %>" selected>dairy</option>

        </select>
        <button>Submit</button>
    </form>
</body>
</html>